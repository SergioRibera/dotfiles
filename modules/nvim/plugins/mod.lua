require('lsp-progress').setup({ max_size = 80 })

-- Screenshots
require('codeshot').setup ({
  copy = "%c | wl-copy",
  shadow_image = true,
  use_current_theme = true,
  background = '#AAAAFF',
  author = '@SergioRibera',
  author_color = '#000',
})
