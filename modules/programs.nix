{...}: {
    programs = {
        fish.enable = true;
        thunar.enable = true;

        hyprland = {
            enable = true;
        };

        neovim = {
            enable = true;
            defaultEditor = true;
        };
    };
}
