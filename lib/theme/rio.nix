colors: toToml:
toToml {
  colors = {
    background = colors.base00;
    foreground = colors.base05;

    # Selection
    selection-background = colors.base02;
    selection-foreground = colors.base05;

    # Navegation
    tabs-active = colors.base02;
    tabs-active-foreground = colors.base05;
    tabs-active-highlight = colors.base0D;
    bar = colors.base01;
    cursor = colors.base05;
    vi-cursor = colors.base0E;

    # Search
    search-match-background = colors.base03;
    search-match-foreground = colors.base05;
    search-focused-match-background = colors.base0A;
    search-focused-match-foreground = colors.base00;

    # Regular Colors
    black = colors.base00;
    blue = colors.base0D;
    cyan = colors.base0C;
    green = colors.base0B;
    magenta = colors.base0E;
    red = colors.base08;
    tabs = colors.base01;
    white = colors.base05;
    yellow = colors.base0A;

    # Dim Colors
    dim-black = colors.base00;
    dim-blue = colors.base0D;
    dim-cyan = colors.base0C;
    dim-foreground = colors.base03;
    dim-green = colors.base0B;
    dim-magenta = colors.base0E;
    dim-red = colors.base08;
    dim-white = colors.base05;
    dim-yellow = colors.base0A;

    # Light Colors
    light-black = colors.base03;
    light-blue = colors.base0D;
    light-cyan = colors.base0C;
    light-foreground = colors.base06;
    light-green = colors.base0B;
    light-magenta = colors.base0E;
    light-red = colors.base08;
    light-white = colors.base07;
  };
}
